# Continue Line Comment

An IntelliJ plugin that lets you press **Shift+Enter** inside a `//` line comment to automatically continue the comment on the next line, preserving indentation.

## Supported languages

Works for any language that uses `//` for line comments: Java, Kotlin, JavaScript, TypeScript, C, C++, Rust, Go, Swift, etc.
